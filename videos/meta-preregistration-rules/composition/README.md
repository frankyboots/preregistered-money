# composition/

One HTML file per scene (CONCEPT §4). Scenes are mounted from `../index.html`
via `data-composition-src`. Numbers on screen come from `../data/` inputs
(derived from the cited results doc), never typed into markup.
