# data/

Small derived data inputs + sources + checksums (CONCEPT §4, video-commit rule 6).
Every on-screen number traces to a cited results doc via the spec's number-citation
table; no figure is computed in a composition. Large datasets never land here.
