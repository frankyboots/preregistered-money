PHASE C (meta-preregistration-rules): 01 c01c474 · 02 c0c0bca · 03 b8fcd00 + FIX cb921c3 (owner nits: row text top 17→14px so each row's text center lines up with its SEALED tag within 1.5px; data-row blank · placeholder replaced by value "named, not shown" typed @39.68 — states the dataset-is-named intent, no data/number; comp 6013fe39, render 29f50e1d byte-identical on comment-only re-render) · 04 5b8fbc4 + FIX node-labels centered (.node-label lacked width → shrink-to-fit flex no-op; width:240px) + SCOREBOARD line split 2-line to clear 3px collision with VERDICT line (gap now 62–68px); comp 9b103d1d, render e75a472c · OPEN: arrow legs x360/x1560 run through PREREG/SCOREBOARD sub-lines (pre-existing, owner flagged) · 05 580e036 + FIX a0053d3 (owner nit: amber SHA 22px→18px local override so the hash fits INSIDE the thin inner ring — at 22px ink 532px vs inner-ring chord ~501px @dy=111, ~13px crossover/side; now 436px ink, 32/33px clearance/side, verified on re-render; template _scene-05.tmpl.html patched + comp regenerated via _build_scene05.py, spec amended, build-log entry; no timing changes — top 699/cues/ring untouched; verify harness ALL PASS, hold sha a032002f) · 06 (bar) b1aabc0 · 07 (ledger) cee1c81 (comp fdf5814e) · 08 (disclaimer) 48d77f8 + FIX (owner nit: bullet wrap line sat at marker x204 not first word x240, 36px; fixed in the PIN — setpieces.css hanging indent `li{padding-left:2ch;text-indent:-2ch}`, style spec 1.1.1→2.0.0 major + owner decision; comp unchanged 5f19ce9e, render 49551d7f 724f, all 7 lines L=240, line-1 wraps byte-identical (1px yuv420p), middot gutter x208–239, hold 1 sha, band 0; snapshot refreshed, probes deleted) · 09 (outro) b2ac245 (window 235.60–246.96 = master tail, comp 5e787ebe; caption band 0px all 273 frames; verbatim lines diff-OK vs beat-09). NEXT: PHASE D — all 9 scenes built; master assembly: index.html root data-duration still scaffold "10"; final full-video render + audio mux + captions. Phase A for video 2 blocked on research side (docs/preregistrations+docs/results don't exist yet; PR-2026-001 pre-draft). Seal SHA=data/spec-sha.txt (86140f9…); captions bottom 132px band soft SRT; Zane reserved; P&L never red/green. LESSONS: (box-sizing) a standalone scene composition needs its own `*{box-sizing:border-box}` — the master's global reset doesn't reach it; without it, inline-block .cell spans pull the strut into table rows (56→77px) and every pinned y drifts. (collapsed borders) a pinned `border-top` on a hidden tr renders through `tr{opacity:0}` — Chrome draws collapsed borders at the table grid; fix = color-transparent structural border (keeps geometry) + an absolutely-positioned overlay div revealed on cue. (sweep memory) never materialize L=raw.max(axis=3) over 1000 full-res frames (2.2GB → kernel OOM); keep the memmap and index per-zone slices. (sweep zones) threshold text vs 35%-dim rules (255 vs 49, thr 60) and keep zones off full-width rule rows — the table wipe sweeps dim rules through row bands and fake-reads as onsets. (h264 fringing) isolated warm-chroma px (≤3/window) near bright text are encoder fringing, not content — density-test, don't pixel-count. (memory tool) replace swaps the WHOLE tracker — content = full new text; verify size after. (dash-draw) dasharray = measured path length; frame-rect axes use perimeter. (SVG) brand-mark.svg viewBox 800/r330 → 640px box = 528px ring; 776px box for a 640px ring. (reveals) never reveal child spans under a parent at opacity:0 — reveal the whole element. (ffmpeg -ss) returns first frame AT/after t — verify negative-proof frames land pre-cue by index. --ease-out is front-loaded (~83% at 24% of 250ms): proof cells describe the measured curve; "not visible before cue" is the hard bar. (terminal timeouts) a 30s-scene draft render takes ~30s but tool timeouts kill it silently mid-encode (build.log gets an exit=? line) — run scene renders as background+notify, then wait on the session. (no-number check) "no digits" must scan rendered TEXT nodes only (bs4 get_text), never the raw HTML — SVG geometry + CSS px values are not on-screen numbers; the pinned corner bug legally carries the date+seal SHA. (vertical align) a row's 30px mono text vs a 22px tag on the same row: text center ≈ top+caps/2 (caps≈0.7em); to line up with a 22px tag's box center, the 30px text top ≈ tag_top + tag_h/2 − caps/2. Measure optical centers on the render (PIL bbox per zone, thr ~140 for --paper, ~85 for --paper-dim) rather than trusting line-height math. (seal geometry) the seal scene's hash fits must be checked against the THIN INNER ring's chord (r=273.5 at the line's dy), not the outer ring — the spec's original "fits" estimate used the outer chord and the hash still crossed the inner ring; measure chord = 2√(r²−dy²) at the line center's dy.