VIDEO 1 (meta-preregistration-rules): THUMB + PUBLISH KIT DONE (2026-09-14) — master render master-full-002 5928f/24fps/246.96s high (33566933…), mux master-full-002-audio.mp4 aac mono 192k (c8ff6fc3…), timeline-manifest output → 002 · assembly commit 5ffd2ec (001 superseded, retained on disk) · scene-04 arrow side-elbow reroute b6e1ca2 · verified: pre-cue zero arrow ink (master 86.30), draw SCOREBOARD→PREREG, regression 0.0000% outside s04 / 0.14% inside, lint 0/0, theme 0 findings, boundary+edge frames 0.0000% vs 001 · THUMBNAIL: look E (seal stamped) from pinned generator brand/gen_thumbnails.py (two looks C2+E, owner 2026-09-14; round1 A/B/C + D rejected — concepts on disk not committed) · thumbnail.html (b46d552a…), renders/thumbnail.png (2af79c22…, gitignored like all renders) · QA: SHA amber line 155..525 vs ring 154..525, tagline center 896 vs 898, hex audit 4 tokens, theme scan clean, vision clean · build.log "## Thumbnail" section added · PUBLISH KIT (commits 07a80c3, e98ed55, 089d585): videos/<slug>/description.md (cites spec seal SHA; repo-link placeholder until GitHub push) + channel description brand/channel-description.md (790/1000 chars) + root README + LICENSE (Apache-2.0; owner 2026-09-14: full reproducibility story public, agents/ as-is) + PROCESS: per-video description now written at the thumbnail stage (channel-art "Video description" + playbook stage 11 + quality gate) · build.log "## Publish kit". NEXT: OWNER REVIEW of renders/master-full-002-audio.mp4 (+ thumbnail.png) + repo presentability (open GitHub, confirm public-ready) → PUBLISH (upload mp4 + captions.srt soft + thumbnail.png + description.md; fill repo link in description; publish record in build.log; tracker → PUBLISH). Lifecycle …REVIEW→FINAL→THUMB→PUBLISH. VIDEO 2 Phase A blocked on research side (docs/preregistrations+results don't exist yet; PR-2026-001 pre-draft). Seal SHA=86140f927343296bb2efc390288ac56b2c9865e2 (data/spec-sha.txt); captions bottom 132px soft SRT; Zane reserved; P&L never red/green. LESSONS: (mount nesting) master mounts must be CHILDREN of the root composition div — as siblings the compiler ignores root data-duration and renders at the first mount's duration (v0.8.35: 9-scene master → 2.69s); root data-duration = beat-map master duration; verify compiler log durationSeconds/totalFrames after render. (box-sizing) a standalone scene composition needs its own `*{box-sizing:border-box}` — the master's global reset doesn't reach it; without it, inline-block .cell spans pull the strut into table rows (56→77px) and every pinned y drifts. (collapsed borders) a pinned `border-top` on a hidden tr renders through `tr{opacity:0}` — Chrome draws collapsed borders at the table grid; fix = color-transparent structural border (keeps geometry) + an absolutely-positioned overlay div revealed on cue. (dash-draw) dasharray = measured path length; frame-rect axes use perimeter; a mis-measured path (2152 vs 2228) wraps the pattern and final units render pre-cue — vision can MISS the dim stub; region-scan at the DIM threshold (~50) on the pre-cue frame, not just >90. (SVG) brand-mark.svg viewBox 800/r330 → 640px box = 528px ring; width/height must match the viewBox ratio (1:1) or stroke weights distort. (reveals) never reveal child spans under a parent at opacity:0 — reveal the whole element. (ffmpeg -ss) returns first frame AT/after t — verify negative-proof frames land pre-cue by index. --ease-out is front-loaded (~83% at 24% of 250ms): proof cells describe the measured curve. (terminal timeouts) a 30s-scene draft render takes ~30s but tool timeouts kill it silently mid-encode — run scene renders background+notify, then wait. (no-number check) scan rendered TEXT nodes only (bs4 get_text), never raw HTML — SVG geometry + CSS px are not on-screen numbers; the pinned corner legally carries date+seal SHA. (vertical align) 30px mono vs 22px tag: text center ≈ top+caps/2 (caps≈0.7em); measure optical centers on the render (PIL bbox per zone, thr ~140 paper / ~85 dim) rather than line-height math. (seal geometry) check hash fits against the THIN INNER ring's chord r=273.5 at the line's dy (chord=2√(r²−dy²)), not the outer ring. (text-to-shape fit) font-math (advance × chars) mispredicts rendered width once letter-spacing/AA enter — render + PIL bbox scan over the FULL row range (one scanline misses thin glyphs) and tune size/tracking; single-scanline amber check on the SHA line gave false 206..517, full-range gave true 157..524. (f-strings/CSS) a CSS unit inside an f-string expression is a SyntaxError ({y+34px}; unit goes outside braces); a left-anchored div with text-align:right just left-aligns — anchor with right:. (video description) per-video description is written at the THUMBNAIL stage (after final MP4 committed, before publish) and committed as videos/<slug>/description.md with the build log; channel description lives at brand/channel-description.md; same number-trace rule as frames — a meta episode cites the seal SHA, not a number. (scan_theme_colors.py) takes ONE root + palette as --allow (quote hexes; unquoted # = shell comment) — without --allow every color is "review" and exit=1 (unconfigured, not a violation). (memory tool) replace swaps the WHOLE tracker — content = full new text; verify size after. (build-log external writer) render.sh auto-appends Render-history during renders — "file modified since read" patch warnings expected; re-read before next patch. (sha hygiene) "replaces X" values come from `git show HEAD:path | sha256sum`, never recalled from memory. (thumbnail look) A/B/C (minimal) rejected "nobody will click"; C2+E pinned: verdict videos → C2 (chart+number), meta → E (seal); meta videos have NO number, a chart would be a lie.
