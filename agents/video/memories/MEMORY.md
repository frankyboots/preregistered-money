PHASE C: scene-01 (title) c01c474; scene-02 (loop) c0c0bca; scene-03 (prereg fields) b8fcd00; scene-04 (lifecycle) 5b8fbc4; scene-05 (seal) 580e036; scene-06 (bar) DRAFT built+verified in this commit — next action: scene-07 (ledger) via `scene-build-and-commit` (spec `scene-specs/scene-07.md` in dfcf99b; window 162.06–205.47, beat-07 speech ends 204.20; template `meta:ledger` §4.7; example rows labeled `example` — no results data exists yet). scene-06 evidence: lint 0; check pass (runtime/layout/motion 0, contrast 7/7); theme scan 0; 13 pre-naming negatives at verified pre-cue frames all 0 non-ink; frame-exact onset sweep (804-frame rawvideo + numpy): every element first lit at cue frame +0/+1 (front-loaded --ease-out class); amber audit: only amber = the two bar rows (3156/2689 px), elsewhere 0; no tick labels (6 axis zones 0 non-ink); caption band y948–1080 0 non-ink; hold 32.83/33.20/33.45 byte-identical (raw frames 777–804 identical; one 917px codec micro-block between 32.167/32.375, max diff 6/255 — encoder plateau, content-identical). Spec amendment (no design change, in commit): `example` box y≈424–444 (the spec's own known-risk numbers) → top y=400 h 28 — the spec numbers put the box bottom border through the tag glyphs ("OOS CAGR"); normative "directly above the tag" wins; probe-measured clearances cap→box 18.5px, box→tag 6px, tag→bar 30px. LESSON (dash-draw): dasharray = measured path length; frame rects used as axes draw with dasharray = perimeter; gridlines fade with the frame group, not a separate draw. LESSON (SVG sizing): brand-mark.svg viewBox=800, outer r=330 → a 640px box draws a 528px ring; box = 640×800/660 = 776px for a 640px drawn ring. LESSON: an opacity reveal on child spans never shows text under a parent at opacity:0 (hide-y) — reveal the whole element (scene-05 SHA). LESSON: ffmpeg -ss returns the first frame AT/after the time — a negative proof time 20ms before a cue lands ON the CUE frame; pre-naming proofs must use a time that verifies as the pre-cue frame (frame index < cue frame). LESSON: h264 puts warm chroma fringing on bright text — an amber pixel audit needs threshold/density (sub-threshold px near bright curves are fringing, not content); scan_theme_colors.py is the acceptance tool. LESSON: 40%-opacity gridlines (lum ≈58) contaminate zone-based onset sweeps — sweep at thr>80 or zone-clear the gridline rows. LESSON: pure-Python per-pixel loops over 800 full-res frames OOM the execute_code kernel — dump `ffmpeg -f rawvideo -pix_fmt rgb24` once + numpy memmap. PHASE B complete: specs 01-09 (01-03: 437cce9; 04: 578f9f7; 05: 1fd9072; 06 bar: 148bb06; 07 ledger: dfcf99b; 08: a8669e7; 09: 895a6a7); 9 windows sum to master 246.96. scene-07 §4.7 flag resolved, ratified in style-spec 1.1.1. PHASE C entry = `scene-build-and-commit` (one scene per commit, never batch); craft → `hyperframes-scene-builder`; commit → `video-commit`. LESSON: --ease-out (power2.out) is front-loaded (≈83% opaque at 24% of a 250ms reveal) — proof cells describe the measured curve; "not visible before cue" is a hard bar. LESSON: tracker rides in the scene commit — never hardcode that commit's own SHA; the `memory` tool replace swaps the WHOLE tracker (content = full new text). Seal SHA = data/spec-sha.txt (86140f9…); P&L colors never red/green; captions bottom 132px band, soft SRT. Zane reserved; stability no-op on Helen. Verified 2026-09-13: no video 2 — meta-preregistration-rules is the only video; docs/preregistrations + docs/results don't exist yet (PR-2026-001 pre-draft), so Phase A for the next video is blocked on the research side.