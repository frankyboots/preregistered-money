**PHASE A DONE 2026-09-13 (commits e08e974, 77f8791, 516de6e):** style spec 1.1.0 + vendored fonts wired as a committed per-video snapshot (pipeline/snapshot_style.sh; new_video.sh runs it); index.html links style/*.css; probe render verified pinned set pieces + Inter/JetBrains Mono + inline brand mark in a real render; lint 0/0, check passed, empty-master draft render OK. Key findings now in hyperframes-scene-builder skill: no ../ paths (lint), no <img> for SVG (media preflight), root-relative assets, snapshot = pipeline SHA in build.log. NEXT SESSION — PHASE B SCENE SPECS: 1) 9 scene specs from beat sheet + words.json cues (durations fixed by beat-map.json; gap 1.25s; master 246.96s); 2) commit spec + scene specs; then Phase C: build batches (title/loop/prereg → life-cycle/seal/bar → ledger/disclaimer/outro), draft render + proof frames per batch, commit per batch with build-log entry. Seal scene SHA = data/spec-sha.txt (86140f9…); P&L colors never red/green; captions bottom 132px band, soft SRT. Zane reserved; stability no-op on Helen.
