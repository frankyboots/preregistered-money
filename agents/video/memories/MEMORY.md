VIDEO 1 (meta-preregistration-rules): PUBLISHED 2026-09-15 — YouTube (mp4 + soft captions.srt + thumbnail + description w/ repo link) + repo public https://github.com/frankyboots/preregistered-money (origin/main 8fd3d2c at publish; channel "about" text pinned at brand/channel-description.md — Studio setting NOT confirmed by owner, flag if still empty) · master-full-002 246.96s/24fps high (33566933…), mux -audio aac mono 192k (c8ff6fc3…), output→002; QA passed (build.log) · THUMB look E (seal) from pinned brand/gen_thumbnails.py; thumbnail.html b46d552a…, renders/thumbnail.png 2af79c22… (gitignored) · PUBLISH KIT (07a80c3, e98ed55, 089d585): <slug>/description.md (cites spec seal SHA; repo link placeholder until push) + brand/channel-description.md (790/1000 chars) + root README + LICENSE Apache-2.0 (owner: full story public incl. agents/) + PROCESS: video description written at thumbnail stage (channel-art + playbook) · build.log "## Publish kit". NEXT: VIDEO 2 Phase A on research-side unblock (docs/preregistrations+results must exist; PR-2026-001 pre-draft → `new_video.sh <slug>` + spec commit per video-bootstrap); no video-side blocker. VIDEO 2: PR-2026-001 pre-draft on research side. Seal SHA=86140f927343296bb2efc390288ac56b2c9865e2 (data/spec-sha.txt); captions bottom 132px soft SRT; Zane reserved; P&L never red/green. LESSONS: (mount nesting) master mounts must be CHILDREN of the root composition div — as siblings the compiler ignores root data-duration and renders at the first mount's duration (v0.8.35: 9-scene master → 2.69s); root data-duration = beat-map master duration; verify compiler log durationSeconds/totalFrames after render. (box-sizing) a standalone scene composition needs its own `*{box-sizing:border-box}` — the master's global reset doesn't reach it; without it, inline-block .cell spans pull the strut into table rows (56→77px) and every pinned y drifts. (collapsed borders) a pinned `border-top` on a hidden tr renders through `tr{opacity:0}` — Chrome draws collapsed borders at the table grid; fix = color-transparent structural border (keeps geometry) + an absolutely-positioned overlay div revealed on cue. (dash-draw) dasharray = measured path length; a mis-measured path wraps the pattern and final units render pre-cue — vision misses the dim stub; region-scan at the DIM threshold (~50) on the pre-cue frame, not just >90. (SVG) brand-mark.svg viewBox 800/r330 → 640px box = 528px ring; width/height must match the viewBox ratio (1:1) or stroke weights distort. (reveals) never reveal child spans under a parent at opacity:0 — reveal the whole element. (ffmpeg -ss) returns first frame AT/after t — verify negative-proof frames land pre-cue by index. --ease-out is front-loaded (~83% at 24% of 250ms): proof cells describe the measured curve. (terminal timeouts) a 30s-scene draft render takes ~30s but tool timeouts kill it silently mid-encode — run scene renders background+notify, then wait. (no-number check) scan rendered TEXT nodes only (bs4 get_text), never raw HTML — SVG geometry + CSS px are not on-screen numbers. (vertical align) measure optical centers on the render (PIL bbox per zone), not line-height math (text center ≈ top+caps/2). (seal geometry) check hash fits against the THIN INNER ring's chord (r=273.5, chord=2√(r²−dy²) at the line's dy), not the outer ring. (text-to-shape fit) font-math mispredicts rendered width once letter-spacing/AA enter — render + PIL bbox scan over the FULL row range (one scanline misses thin glyphs), then tune size/tracking. (f-strings/CSS) a CSS unit inside an f-string expression is a SyntaxError ({y+34px}; unit goes outside braces); a left-anchored div with text-align:right just left-aligns — anchor with right:. (video description) per-video description is written at the THUMBNAIL stage (after final MP4 committed, before publish) and committed as videos/<slug>/description.md with the build log; channel description lives at brand/channel-description.md; same number-trace rule as frames — a meta episode cites the seal SHA, not a number. (scan_theme_colors.py) takes ONE root + palette as --allow (quote hexes; unquoted # = shell comment) — without --allow every color is "review" and exit=1 (unconfigured, not a violation). (memory tool) NO substring mode — replace swaps the ENTIRE entry even when old_string matches a substring; always read current state, compose the FULL new text, verify size after (sudden shrink = clobber; git show HEAD:agents/video/memories/MEMORY.md restores). (build-log external writer) render.sh auto-appends to build.log — "file modified since read" patch warnings expected; re-read before next patch. (sha hygiene) "replaces X" values from `git show HEAD:path | sha256sum`, never memory. (thumbnail look) C2+E pinned: verdict → C2 (chart+number), meta → E (seal); a meta episode has NO number — a chart would be a lie.
