Tracker 2026-09-12 — repo /home/frank/preregistered-money. In-flight: first video = Phase 0 Meta explainer `videos/meta-preregistration-rules/` — **SPEC committed** (86140f9, 9 beats, 3:30–4:00, citations CONCEPT+METHODOLOGY, example values labeled). Style system DECIDED+COMMITTED (d2defb8): style-spec v1.0.0 (7-token semantic palette ink/paper/amber + 3 verdict colors; Inter + JetBrains Mono, mono-numbers rule; pinned set pieces: bug / seal scene / bar / verdict card / disclaimer card / ledger; five motion verbs; narration-anchored reveal rule replaces static-hold ban: reveal-on-naming, no pre-naming >2s, change floor 1 reveal/10s speech, stillness only in final hold) + tokens.css + setpieces.css + seal.svg placeholder + fonts vendored (Inter 400/600/700, JBM 400/500 WOFF2, checksums in commit). Done earlier: agent scaffold + 11 video skills; pipeline QA tools + spec templates; METHODOLOGY v1.1; hyperframes scaffolding (skeleton, render.sh pins HF 0.8.35, new_video.sh). TODO (build order): 1) pin data/spec-sha.txt (SHA of 86140f9) before first render; 2) wire tokens.css+setpieces.css @imports into skeleton/composition; 3) TTS glue (ElevenLabs v3, no creds yet — blocking first render); 4) Meta template family v1 beats (title-card, loop-diagram, prereg-fields, life-cycle, bar-chart, aggregate-rows, outro) + first DRAFT render. Next session: TTS creds → glue → draft. Open owner decisions: channel name/handle (blocks wordmark, not build); P&L no-red/green (spec'd, confirm); seal SFX (spec'd: none); captions (spec'd: always); slug convention (used meta- prefix, settles at video #2); request to research: emit scoreboard.json; CONCEPT §6/7 pointer to VIDEO_CONCEPT.
