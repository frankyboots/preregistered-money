Tracker 2026-09-12 — repo /home/frank/preregistered-money. In-flight: (1) channel art — **banner v2 committed (3fff9bc)**: no channel name on banner (owner: YouTube already renders it on-page); chart + `bar: pre-registered` + tagline as hero text; tagline now the only words on the art; logo unchanged; `concepts/bar` re-rendered too (same generator); hex audit clean. **Channel name settled 2026-09-12: Preregistered Money, @preregisteredmoney (already claimed)** — locked into style-spec v1.1.0 (new §4.2 brand mark, `style/brand-mark.svg` replaces seal.svg, bug + seal scene use the mark, wordmark `PREREGISTERED MONEY`), VIDEO_CONCEPT §11, pipeline README, channel-art skill, first-video spec (pinned 1.1.0). Remaining: owner uploads banner.png/logo.png to YouTube (manual act); CONCEPT §11 line 3 (research side) still lists naming as open — flag to owner/research. (2) first video = Phase 0 Meta explainer `videos/meta-preregistration-rules/` — SPEC committed (86140f9), style system v1.1.0. TODO (build order): 1) spec-sha.txt pinned (fea532b); 2) wire tokens.css+setpieces.css into skeleton; 3) TTS glue (ElevenLabs v3, no creds — blocks first render); 4) Meta template family v1 beats + first DRAFT render (spec beat 1 now uses the real wordmark + brand mark). Next: TTS creds → glue → draft. Open owner decisions: P&L no-red/green (spec'd, confirm); seal SFX (spec'd: none); captions (spec'd: always); slug convention (settles at video #2); request to research: emit scoreboard.json; CONCEPT §6/7 pointer to VIDEO_CONCEPT; CONCEPT §11 naming line (research side).
