PHASE C (meta-preregistration-rules): 01 c01c474 · 02 c0c0bca · 03 b8fcd00 · 04 5b8fbc4 · 05 580e036 · 06 DRAFT (bar, 128.57–162.06) · 07 (ledger) DRAFT built+verified THIS commit (window 162.06–205.47, comp sha fdf5814e, rules 328/384/440/495-496/552, 19/19 onsets cue+0/+1, hold byte-identical, chroma confined to verdict cells+agg2, theme-scan 0). NEXT: scene-08 (disclaimer card, spec a8669e7; opens from ink; hard cut in from scene-07) via scene-build-and-commit; then scene-09 (895a6a7). All 9 specs committed (PHASE B done; 9 windows sum to master 246.96). Phase A for video 2 blocked on research side (docs/preregistrations+docs/results don't exist yet; PR-2026-001 pre-draft). Seal SHA=data/spec-sha.txt (86140f9…); captions bottom 132px band soft SRT; Zane reserved; P&L never red/green. LESSONS: (box-sizing) a standalone scene composition needs its own `*{box-sizing:border-box}` — the master's global reset doesn't reach it; without it, inline-block .cell spans pull the strut into table rows (56→77px) and every pinned y drifts. (collapsed borders) a pinned `border-top` on a hidden tr renders through `tr{opacity:0}` — Chrome draws collapsed borders at the table grid; fix = color-transparent structural border (keeps geometry) + an absolutely-positioned overlay div revealed on cue. (sweep memory) never materialize L=raw.max(axis=3) over 1000 full-res frames (2.2GB → kernel OOM); keep the memmap and index per-zone slices. (sweep zones) threshold text vs 35%-dim rules (255 vs 49, thr 60) and keep zones off full-width rule rows — the table wipe sweeps dim rules through row bands and fake-reads as onsets. (h264 fringing) isolated warm-chroma px (≤3/window) near bright text are encoder fringing, not content — density-test, don't pixel-count. (memory tool) replace swaps the WHOLE tracker — content = full new text; verify size after. (dash-draw) dasharray = measured path length; frame-rect axes use perimeter. (SVG) brand-mark.svg viewBox 800/r330 → 640px box = 528px ring; 776px box for a 640px ring. (reveals) never reveal child spans under a parent at opacity:0 — reveal the whole element. (ffmpeg -ss) returns first frame AT/after t — verify negative-proof frames land pre-cue by index. --ease-out is front-loaded (~83% at 24% of 250ms): proof cells describe the measured curve; "not visible before cue" is the hard bar.