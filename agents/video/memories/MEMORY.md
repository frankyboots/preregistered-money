Tracker 2026-09-12 — repo /home/frank/preregistered-money. In-flight: no videos yet; first video = Phase 0 Meta explainer (videos/VIDEO_CONCEPT §10). Done: agent scaffold + 11 video skills; pipeline QA tools + spec templates; METHODOLOGY v1.1; Apache-2.0 provenance; hyperframes scaffolding DECIDED+COMMITTED: skeleton at videos/pipeline/skeleton (new video = copy via pipeline/new_video.sh, NEVER `hyperframes init` in repo), render.sh pins HF_VERSION=0.8.35 + HYPERFRAMES_SKIP_SKILLS=1, relative --output → <video>/renders/ (gitignored; proof frames = committed QA), renders/ + snapshots/ gitignored. TODO: style-spec v1 + Meta template family; TTS glue (ElevenLabs v3); first spec.md for meta explainer (spec gates render). Next: style-spec → spec.md → first video. Open owner decisions: channel name/handle; slug convention (prop: series-prefix); captions (prop: always); request to research: emit scoreboard.json; CONCEPT §6/7 pointer.
§
Env: local LLM Qwen3.8-27B-FP8 @ localhost:8000/v1 (vision on); TTS not configured (ElevenLabs planned, no creds yet). Dual-agent repo: video side may commit only videos/**, agents/video/**, video-render CI. memories/ + skills/ dirs are symlinks into the repo (shared with research-side setup), so memory/skill writes are reviewable in git.