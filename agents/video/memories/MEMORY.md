PHASE B COMPLETE: scene specs 01-09 all DONE (01-03: 437cce9, 04: 578f9f7, 05: 1fd9072; 06: bar 148bb06; 07: ledger dfcf99b; 08: disclaimer a8669e7; 09: outro 895a6a7). 09's window ends at master_duration_s 246.96; all 9 windows sum to master. scene-07 §4.7 flag RESOLVED (owner 2026-09-13): column-unit reveals accepted when narration enumerates columns; ratified in style-spec 1.1.1 (patch, prose-only; tokens/setpieces byte-identical to 1.1.0 snapshot). PHASE C now has its single entry-point skill `scene-build-and-commit` (one scene built from its committed spec → draft render → proof frames vs the spec's QA checklist → build.log line → commit; NEVER batch, one scene per commit, mirrors `scene-spec-and-commit`). It delegates composition craft to `hyperframes-scene-builder`, commit to `video-commit`; playbook + bootstrap + scene-spec-and-commit + scene-builder all re-pointed at it ("skill pending" gone). LESSON: tracker rides in the scene-spec commit — never hardcode that commit's own SHA (self-reference breaks on amend); the memory tool's replace swaps the WHOLE tracker entry (old_text only identifies it) — content must be the full new text. NEXT (owner: builds start next session): Phase C, ONE SCENE AT A TIME via `scene-build-and-commit`, starting scene-01 (build composition from spec, draft render, proof frames, build log append, commit per video-commit). Seal SHA = data/spec-sha.txt (86140f9…); P&L colors never red/green; captions bottom 132px band, soft SRT. Zane reserved; stability no-op on Helen. Verified 2026-09-13: no video 2 exists — meta-preregistration-rules is the only video; docs/preregistrations + docs/results don't exist yet (PR-2026-001 still pre-draft on the research side), so Phase A for the next video is blocked on the research side publishing a prereg/results doc.