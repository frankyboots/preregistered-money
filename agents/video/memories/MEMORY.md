PHASE C: scene-01 (title) c01c474; scene-02 (loop) c0c0bca; scene-03 (prereg fields) DRAFT verified; scene-04 (lifecycle diagram) 5b8fbc4; scene-05 (seal) built+verified DRAFT in this commit — next action: scene-06 via `scene-build-and-commit`. scene-05 evidence: lint 0; check pass (runtime/layout/motion 0, contrast 7/7); theme scan 0; 23 proof frames pass (8 pre-naming negatives at pre-cue frames, caption band y948–1080 = 0 non-ink, hold 32.76/33.00/33.20 byte-identical); audio-sync cmd lands 111.65≤112.30, SHA settled 114.41≤114.88. Spec amendment (no design change, in commit): SHA y≈700 → top y=699/center ≈711 (line-endpoint round cap measured 5px into the glyph tops; +10px clears it, 6px clearance, still inside the ring — 72px chord slack). LESSON (SVG sizing): brand-mark.svg viewBox=800, outer r=330 → a 640px box draws a 528px ring; box = 640×800/660 = 776px for a 640px drawn ring. LESSON: an opacity reveal on child spans never shows text under a parent at opacity:0 (hide-y) — reveal the whole element, as scene-04 does (the spec's "reveals" verb + the t=114.44 proof cell agree; a type verb settles ~115.14, after "SHA" ends 114.88). LESSON: ffmpeg -ss returns the first frame AT/after the time — a negative proof time 20ms before a cue lands on the CUE frame; pre-naming proofs must use a time that verifies as the pre-cue frame. LESSON: h264 puts warm chroma fringing on white text — an amber pixel audit needs density (the SHA fill ≈77 px/24×24 vs fringing ≤23); scan_theme_colors.py is the acceptance tool. PHASE B complete: specs 01-09 (01-03: 437cce9; 04: 578f9f7; 05: 1fd9072; 06 bar: 148bb06; 07 ledger: dfcf99b; 08: a8669e7; 09: 895a6a7); 9 windows sum to master 246.96. scene-07 §4.7 flag resolved, ratified in style-spec 1.1.1. PHASE C entry = `scene-build-and-commit` (one scene per commit, never batch); craft → `hyperframes-scene-builder`; commit → `video-commit`. LESSON: --ease-out (power2.out) is front-loaded (≈83% opaque at 24% of a 250ms reveal) — proof cells describe the measured curve; "not visible before cue" is a hard bar. LESSON: tracker rides in the scene commit — never hardcode that commit's own SHA; the `memory` tool replace swaps the WHOLE tracker (content = full new text). NEXT: Phase C scene-06 (spec `scene-specs/scene-06.md` in 148bb06; the bar — bar-chart template; continuity-in = scene-05 final frame, hard cut; window 128.57–162.06, beat-06 narration ends 160.81). Seal SHA = data/spec-sha.txt (86140f9…); P&L colors never red/green; captions bottom 132px band, soft SRT. Zane reserved; stability no-op on Helen. Verified 2026-09-13: no video 2 — meta-preregistration-rules is the only video; docs/preregistrations + docs/results don't exist yet (PR-2026-001 pre-draft), so Phase A for the next video is blocked on the research side.