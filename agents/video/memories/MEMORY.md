VIDEO 1 (meta-preregistration-rules): PHASE D DONE (2026-09-14) — all 9 scenes built (fix SHAs: s03 cb921c3 · s04 5fa4c1b · s05 a0053d3 · s08 8a5bab4); master render master-full-001 5928f/24fps/246.96s high (a75afdcc…), mux master-full-001-audio.mp4 aac mono 192k (ac621d69…) — commit 5ffd2ec (root 246.96 + mounts nested in root div + timeline-manifest.json + build log) · skeleton fixed 467e7cc · verified: boundaries = standalone first frames (0.165%), seal ring @111.95 + SHA-in-ring @114.44, ledger @190, outro @246.9. NEXT: OWNER REVIEW of renders/master-full-001-audio.mp4 → PUBLISH (upload + committed captions.srt soft). NOTE: --quality takes draft/standard/high ("final" invalid). VIDEO 2 Phase A blocked on research side (docs/preregistrations+results don't exist yet; PR-2026-001 pre-draft). Seal SHA=86140f9…; captions bottom 132px soft SRT; Zane reserved; P&L never red/green. LESSONS: (mount nesting) master mounts must be CHILDREN of the root composition div — as siblings the compiler ignores root data-duration and renders at the first mount's duration (v0.8.35: 9-scene master → 2.69s); root data-duration = beat-map master duration; verify compiler log durationSeconds/totalFrames after render. (box-sizing) a standalone scene composition needs its own `*{box-sizing:border-box}` — the master's global reset doesn't reach it; without it, inline-block .cell spans pull the strut into table rows (56→77px) and every pinned y drifts. (collapsed borders) a pinned `border-top` on a hidden tr renders through `tr{opacity:0}` — Chrome draws collapsed borders at the table grid; fix = color-transparent structural border (keeps geometry) + an absolutely-positioned overlay div revealed on cue. (dash-draw) dasharray = measured path length; frame-rect axes use perimeter. (SVG) brand-mark.svg viewBox 800/r330 → 640px box = 528px ring; 776px box for a 640px ring. (reveals) never reveal child spans under a parent at opacity:0 — reveal the whole element. (ffmpeg -ss) returns first frame AT/after t — verify negative-proof frames land pre-cue by index. --ease-out is front-loaded (~83% at 24% of 250ms): proof cells describe the measured curve; "not visible before cue" is the hard bar. (terminal timeouts) a 30s-scene draft render takes ~30s but tool timeouts kill it silently mid-encode (build.log gets an exit=? line) — run scene renders as background+notify, then wait on the session. (no-number check) "no digits" must scan rendered TEXT nodes only (bs4 get_text), never the raw HTML — SVG geometry + CSS px values are not on-screen numbers; the pinned corner bug legally carries the date+seal SHA. (vertical align) a row's 30px mono text vs a 22px tag on the same row: text center ≈ top+caps/2 (caps≈0.7em); to line up with a 22px tag's box center, the 30px text top ≈ tag_top + tag_h/2 − caps/2. Measure optical centers on the render (PIL bbox per zone, thr ~140 for --paper, ~85 for --paper-dim) rather than trusting line-height math. (seal geometry) the seal scene's hash fits must be checked against the THIN INNER ring's chord (r=273.5 at the line's dy), not the outer ring — the spec's original "fits" estimate used the outer chord and the hash still crossed the inner ring; measure chord = 2√(r²−dy²) at the line center's dy. (memory tool) replace swaps the WHOLE tracker — content = full new text; verify size after.